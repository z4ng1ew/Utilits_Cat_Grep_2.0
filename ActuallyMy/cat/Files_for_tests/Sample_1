* Флаг -b (нумерация только непустых строк) *

Пример использования:
cat -b example.txt

Пример вывода:
     1  строка первая
     2  строка вторая
     3  строка третья

* Флаг -e (добавление символа $ в конце строк) *

Пример использования:
cat -e example.txt

Пример вывода:
строка первая$
строка вторая$
строка третья$

* Флаг -n (нумерация всех строк) *

Пример использования:
cat -n example.txt

Пример вывода:
     1  строка первая
     2  строка вторая
     3  строка третья

* Флаг -s (удаление пустых строк) *

Пример использования:
cat -s example.txt

Пример вывода:
строка первая
строка вторая
строка третья

* Флаг -v (отображение непечатаемых символов) *

Пример использования:
cat -v example.txt

Пример вывода:
строка первая
строка вторая^@
строка третья

* Флаг -t (отображение табуляций как ^I) *

Пример использования:
cat -t example.txt

Пример вывода:
строка первая
строка вторая^I
строка третья
