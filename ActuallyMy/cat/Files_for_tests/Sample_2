версия файла с тестами для утилиты `cat`,  позволяет тестировать все флаги:

```plaintext
* Флаг -e (добавление символа $ в конце строк) *

Содержимое файла input.txt:
```
Строка один.
Строка два с пробелами в конце    
Строка три с табуляцией в конце\t
```

Ожидаемый результат при выполнении команды `cat -e input.txt`:
```
Строка один.$
Строка два с пробелами в конце    $
Строка три с табуляцией в конце\t$
```

* Флаг -v (отображение непечатаемых символов) *

Содержимое файла input.txt:
```
Строка один.
Строка два с табуляцией\t и переносом строки\n
Строка три с символом возврата каретки\r
```

Ожидаемый результат при выполнении команды `cat -v input.txt`:
```
Строка один.
Строка два с табуляцией\t и переносом строки\n
Строка три с символом возврата каретки\r
```

* Флаг -t (отображение табуляций как ^I) *

Содержимое файла input.txt:
```
Строка один.
Строка два с табуляцией в начале	и середине	и конце	
Строка три без табуляций
```

Ожидаемый результат при выполнении команды `cat -t input.txt`:
```
Строка один.
Строка два с табуляцией в начале^Iи середине^Iи конце^I
Строка три без табуляций
```

* Флаг -n (нумерация всех строк) *

Содержимое файла input.txt:
```
Строка один.
Строка два.
Строка три.
```

Ожидаемый результат при выполнении команды `cat -n input.txt`:
```
     1  Строка один.
     2  Строка два.
     3  Строка три.
```

* Флаг -b (нумерация только непустых строк) *

Содержимое файла input.txt:
```
Строка один.

Строка два.

Строка три.
```

Ожидаемый результат при выполнении команды `cat -b input.txt`:
```
     1  Строка один.

     2  Строка два.

     3  Строка три.
```

* Флаг -s (удаление лишних пустых строк) *

Содержимое файла input.txt:
```
Строка один.

Строка два.


Строка три.
```

Ожидаемый результат при выполнении команды `cat -s input.txt`:
```
Строка один.
Строка два.
Строка три.
```

* Флаг -A (эквивалент опциям -vET) *

Содержимое файла input.txt:
```
Строка один.
Строка два с табуляцией\t и переносом строки\n
Строка три с символом возврата каретки\r
```

Ожидаемый результат при выполнении команды `cat -A input.txt`:
```
Строка один.$
Строка два с табуляцией\t и переносом строки\n$
Строка три с символом возврата каретки\r$
```

```

В этом примере каждая опция `cat` демонстрируется с использованием файла `input.txt` с определенным содержимым и ожидаемым результатом при использовании соответствующей опции. Это позволяет легко и эффективно проверять, как каждая опция обрабатывает различные типы данных и специальные символы.
