=== Опция -b (нумерация непустых строк) ===

Пример команды:
cat -b file.txt

Пример вывода:
     1  первая строка
     2  вторая строка
     3  третья строка

=== Опция -e (добавление символа $ в конец строк) ===

Пример команды:
cat -e file.txt

Пример вывода:
первая строка$
вторая строка$
третья строка$

=== Опция -n (нумерация всех строк) ===

Пример команды:
cat -n file.txt

Пример вывода:
     1  первая строка
     2  вторая строка
     3  третья строка

=== Опция -s (удаление пустых строк) ===

Пример команды:
cat -s file.txt

Пример вывода:
первая строка
вторая строка
третья строка

=== Опция -v (отображение непечатаемых символов) ===

Пример команды:
cat -v file.txt

Пример вывода:
первая строка
вторая строка$
третья строка

=== Опция -t (отображение табуляций как ^I) ===

Пример команды:
cat -t file.txt

Пример вывода:
первая строка
вторая строка^I
третья строка


